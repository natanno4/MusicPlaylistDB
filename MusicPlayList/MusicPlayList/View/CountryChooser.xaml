<Window x:Class="MusicPlayList.View.CountryChooser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicPlayList.View"
        mc:Ignorable="d"
        Title="Country chooser" Height="450" Width="800" Background="#04c582">
    <Grid Margin="0,0,0,-21">
        <Border Background="#2e3137" CornerRadius="20" Margin="30">
            <ListBox BorderThickness="0" Background="Transparent" Name="list" ItemsSource="{Binding Map}" Margin="80,30,77,111" SelectionMode="Extended">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Name="check" IsChecked="{Binding IsChecked, Mode=TwoWay}" Margin="5 5 0 10" VerticalAlignment="Center" />
                            <ContentPresenter Content="{Binding Value}" Margin="5 5 0 10"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <Button IsEnabled="{Binding Path=IsButtonEnabled}" Content="Submit" Margin="355,362,357,53" />

    </Grid>
</Window>
